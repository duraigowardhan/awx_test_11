- name: Hello World Sample
  hosts: all
  vars:
    pc_build1: "{{pc_build|default(groupvars.onprem.builds.pc_build)}}"
  tasks:
    - name: Hello Message
      debug:
        msg: "Hello World by durai11! {{pc_build1}}"
